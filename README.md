# Pasos para ejecutar el proyecto

1 - Leventar el contenedor de Docker
En la carpeta backend/ a la misma "altura" que el archivo docker-compose.yml ejecutar:
```
docker-compose up --build
```

2 - Instalar dependencias
```
npm install
```

3 - Iniciar el servidor de desarrollo
```
npm run dev
```
